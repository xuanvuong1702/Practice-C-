#ifndef GEUN_H
#define GEUN_H
#include <vector>
#include <string>

using namespace std;

vector<int> SplitString(string s, string delimiter);
int CalculateGeun(int n, int k, vector<int> items);

void RunGeun();

#endif
