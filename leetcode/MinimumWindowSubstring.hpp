#include <string>
#ifndef MINIMUM_WINDOW_SUBSTRING
#define MINIMUM_WINDOW_SUBSTRING

using namespace std;

class MinumumWindowSubstring {
public:
    string MinWindow(string s, string t);
    string RemoveDuplicateCharacter(string s);
};

void RunMinumumWindowSubstring();

#endif
